import { View } from "react-native"

export const ProductItem = ({ product }: { product: any }) => {
    return (
        <View style={{
            gap: 15,
            backgroundColor: "#FAFAFF",
            margin: 5,
            borderRadius: 10,
            borderColor: "#0002",
            borderWidth: 1,

            overflow: "hidden",
        }}>
        </View>
    )
}